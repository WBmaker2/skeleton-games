# Game Loop Follow-up Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 카메라·키보드·터치로 실제 플레이 가능한 앱 루프와 폴백·보정소비·배포문서를 구축한다.

**Architecture:** `PoseEngine` 뒤에 `FallbackEngine`(키보드·포인터 합성)을 끼워 게임 코드 무변경 폴백. `GameLoop`가 rAF·dt클램프·`FpsMonitor`·스켈레톤 렌더·이벤트→HUD를 소유. `App`(app.ts 순수 팩토리 + main.ts 배선)이 hash→게임·엔진 선택·보정 오버레이를 담당. 문서는 코드와 함께 기록.

**Tech Stack:** Vite 5 + TypeScript 5 (strict) + Canvas 2D + WebAudio + Vitest (happy-dom pragma per-file for DOM) — 신규 런타임 의존성 없음.

**Spec:** `docs/superpowers/specs/2026-09-19-game-loop-followup-design.md` (부모: `docs/superpowers/specs/2026-09-19-skeleton-game-design.md`)

## Global Constraints

- 설치 없이 URL/QR로 10초 내 시작
- 저사양 크롬북에서 15fps 이상, 판정 지연 <200ms
- 카메라 영상 외부 전송 없음 (온디바이스 처리, 영상·원시 좌표 저장 금지, 점수만 localStorage 저장)
- 앉음(책상) 모드와 선(전신) 모드 모두 지원
- 단일 웹앱, 게임 모드 전환식 (URL hash: `#/fruit`, `#/squat`, `#/abc`, `#/math`)
- 서버 없음 (정적 호스팅)
- 키보드·터치 폴백으로 4종 완주 가능해야 함
- 보정 스킵 시 seated 기본값으로 즉시 시작
- 기존 export 유지: `boot(): void` (src/main.ts), `openCamera(video: HTMLVideoElement): Promise<HTMLVideoElement | null>` (시그니처 변경 금지)
- 기존 테스트 전부 green 유지 (29 tests)

---

### Task 1: FallbackEngine (키보드·포인터 → PoseFrame 합성)

**Files:**
- Create: `src/pose/fallback-engine.ts`
- Test: `tests/fallback-engine.test.ts`

**Interfaces:**
- Consumes: `PoseFrame`, `Keypoint` from `src/pose/types.ts`; `PoseEngine` from `src/pose/pose-engine.ts`
- Produces:
  - `const CURSOR_SPEED = 240` in `src/pose/fallback-engine.ts`
  - `class FallbackEngine implements PoseEngine` with `name='fallback'`, `cursor: { x: number; y: number }`, `moveTo(x: number, y: number): void`, `attach(el: HTMLElement): void`, `detach(): void`, `load(): Promise<void>`, `estimate(_video: HTMLVideoElement): Promise<PoseFrame>`, `dispose(): Promise<void>`

- [ ] **Step 1: Write the failing test**

```ts
// @vitest-environment happy-dom
// tests/fallback-engine.test.ts
import { describe, expect, it } from 'vitest';
import { FallbackEngine } from '../src/pose/fallback-engine';

describe('FallbackEngine', () => {
  it('exposes cursor as right_wrist', async () => {
    const eng = new FallbackEngine();
    await eng.load();
    eng.moveTo(100, 80);
    const video = document.createElement('video');
    const frame = await eng.estimate(video);
    const rw = frame.keypoints.find((k) => k.name === 'right_wrist');
    expect(rw?.x).toBe(100);
    expect(rw?.y).toBe(80);
  });
  it('moves cursor with arrow keys at 240px/s bounds', async () => {
    const eng = new FallbackEngine();
    const el = document.createElement('div');
    document.body.appendChild(el);
    eng.attach(el);
    eng.moveTo(320, 240);
    el.dispatchEvent(new KeyboardEvent('keydown', { key: 'ArrowRight', bubbles: true }));
    const video = document.createElement('video');
    const frame = await eng.estimate(video);
    expect(frame.keypoints.find((k) => k.name === 'right_wrist')?.x).toBeGreaterThan(320);
    eng.detach();
    el.remove();
  });
  it('has all canonical names with score 1', async () => {
    const eng = new FallbackEngine();
    const frame = await eng.estimate(document.createElement('video'));
    for (const n of ['nose', 'left_shoulder', 'right_shoulder', 'left_wrist', 'right_wrist', 'left_hip', 'right_hip']) {
      expect(frame.keypoints.find((k) => k.name === n)?.score).toBe(1);
    }
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run tests/fallback-engine.test.ts`
Expected: FAIL with "Cannot find module '../src/pose/fallback-engine'"

- [ ] **Step 3: Write minimal implementation**

```ts
// src/pose/fallback-engine.ts
import type { Keypoint, PoseFrame } from './types';
import type { PoseEngine } from './pose-engine';

export const CURSOR_SPEED = 240;
const W = 640;
const H = 480;

function clamp(v: number, lo: number, hi: number): number {
  return Math.min(hi, Math.max(lo, v));
}

export class FallbackEngine implements PoseEngine {
  name = 'fallback';
  cursor = { x: 320, y: 240 };
  private el: HTMLElement | null = null;
  private onKey = (e: KeyboardEvent): void => {
    const step = CURSOR_SPEED / 10;
    if (e.key === 'ArrowLeft' || e.key === 'a') this.moveTo(this.cursor.x - step, this.cursor.y);
    else if (e.key === 'ArrowRight' || e.key === 'd') this.moveTo(this.cursor.x + step, this.cursor.y);
    else if (e.key === 'ArrowUp' || e.key === 'w') this.moveTo(this.cursor.x, this.cursor.y - step);
    else if (e.key === 'ArrowDown' || e.key === 's') this.moveTo(this.cursor.x, this.cursor.y + step);
    else if (e.key === ' ') this.moveTo(this.cursor.x, this.cursor.y - 60);
  };
  private onPointer = (e: PointerEvent): void => {
    const rect = (e.target as HTMLElement).getBoundingClientRect?.();
    const x = rect && rect.width ? ((e.clientX - rect.left) / rect.width) * W : e.clientX;
    const y = rect && rect.height ? ((e.clientY - rect.top) / rect.height) * H : e.clientY;
    this.moveTo(x, y);
  };

  async load(): Promise<void> {}
  moveTo(x: number, y: number): void {
    this.cursor = { x: clamp(x, 0, W), y: clamp(y, 0, H) };
  }
  attach(el: HTMLElement): void {
    this.detach();
    this.el = el;
    el.setAttribute('tabindex', '0');
    el.addEventListener('keydown', this.onKey);
    el.addEventListener('pointermove', this.onPointer as EventListener);
  }
  detach(): void {
    this.el?.removeEventListener('keydown', this.onKey);
    this.el?.removeEventListener('pointermove', this.onPointer as EventListener);
    this.el = null;
  }
  async estimate(_video: HTMLVideoElement): Promise<PoseFrame> {
    const kp = (name: string, x: number, y: number): Keypoint => ({ name, x, y, score: 1 });
    return {
      width: W,
      height: H,
      timestamp: performance.now(),
      keypoints: [
        kp('nose', 320, 80),
        kp('left_eye', 310, 72),
        kp('right_eye', 330, 72),
        kp('left_ear', 300, 80),
        kp('right_ear', 340, 80),
        kp('left_shoulder', 270, 120),
        kp('right_shoulder', 370, 120),
        kp('left_elbow', 240, 160),
        kp('right_elbow', 400, 160),
        kp('left_wrist', 220, 200),
        kp('right_wrist', this.cursor.x, this.cursor.y),
        kp('left_hip', 280, 220),
        kp('right_hip', 360, 220),
        kp('left_knee', 280, 320),
        kp('right_knee', 360, 320),
        kp('left_ankle', 280, 420),
        kp('right_ankle', 360, 420)
      ]
    };
  }
  async dispose(): Promise<void> {
    this.detach();
  }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run tests/fallback-engine.test.ts`
Expected: PASS (3 passed)

- [ ] **Step 5: Commit**

```bash
git add src/pose/fallback-engine.ts tests/fallback-engine.test.ts
git commit -m "feat: add fallback engine for keyboard and pointer"
```

---

### Task 2: FpsMonitor + openCamera 폴백 체인

**Files:**
- Create: `src/perf/fps-monitor.ts`
- Modify: `src/main.ts` (openCamera only — boot untouched)
- Test: `tests/fps-monitor.test.ts`
- Test: `tests/camera.test.ts`

**Interfaces:**
- Consumes: 없음 (신규 순수모듈) + 기존 `openCamera(video): Promise<HTMLVideoElement | null>` 시그니처 (Task 9, 유지)
- Produces:
  - `class FpsMonitor { fps: number; degraded: boolean; sample(nowMs: number): void; reset(): void }` in `src/perf/fps-monitor.ts`
  - `openCamera` 동일 시그니처, 내부 시도 순서: 1280x720+facingMode user → 640x480 → null

- [ ] **Step 1: Write the failing tests**

```ts
// tests/fps-monitor.test.ts
import { describe, expect, it } from 'vitest';
import { FpsMonitor } from '../src/perf/fps-monitor';

describe('FpsMonitor', () => {
  it('stays healthy at 60fps', () => {
    const m = new FpsMonitor();
    for (let i = 0; i < 120; i++) m.sample(i * 16.7);
    expect(m.degraded).toBe(false);
    expect(m.fps).toBeGreaterThan(30);
  });
  it('degrades after sustained 5fps', () => {
    const m = new FpsMonitor();
    for (let i = 0; i < 80; i++) m.sample(i * 200);
    expect(m.fps).toBeLessThan(12);
    expect(m.degraded).toBe(true);
  });
  it('reset clears degraded', () => {
    const m = new FpsMonitor();
    for (let i = 0; i < 80; i++) m.sample(i * 200);
    m.reset();
    expect(m.degraded).toBe(false);
    expect(m.fps).toBe(60);
  });
});
```

```ts
// @vitest-environment happy-dom
// tests/camera.test.ts
import { afterEach, describe, expect, it, vi } from 'vitest';
import { openCamera } from '../src/main';

afterEach(() => {
  vi.unstubAllGlobals();
});

describe('openCamera fallback chain', () => {
  it('falls back to 640x480 when 720p fails', async () => {
    const stream720 = { id: 'high' };
    const stream480 = { id: 'low' };
    const getUserMedia = vi
      .fn()
      .mockRejectedValueOnce(new Error('overconstrained'))
      .mockResolvedValueOnce(stream480);
    vi.stubGlobal('navigator', { mediaDevices: { getUserMedia } });
    const video = document.createElement('video');
    video.play = vi.fn().mockResolvedValue(undefined);
    const out = await openCamera(video);
    expect(out).toBe(video);
    expect(getUserMedia).toHaveBeenCalledTimes(2);
    expect(video.srcObject).toBe(stream480);
    void stream720;
  });
  it('returns null when all fail', async () => {
    const getUserMedia = vi.fn().mockRejectedValue(new Error('denied'));
    vi.stubGlobal('navigator', { mediaDevices: { getUserMedia } });
    const video = document.createElement('video');
    video.play = vi.fn().mockResolvedValue(undefined);
    await expect(openCamera(video)).resolves.toBe(null);
  });
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `npx vitest run tests/fps-monitor.test.ts tests/camera.test.ts`
Expected: FAIL with "Cannot find module '../src/perf/fps-monitor'"

- [ ] **Step 3: Write minimal implementation**

```ts
// src/perf/fps-monitor.ts
export class FpsMonitor {
  fps = 60;
  degraded = false;
  private last: number | null = null;
  private lowSince: number | null = null;

  sample(nowMs: number): void {
    if (this.last !== null) {
      const dt = nowMs - this.last;
      if (dt > 0) {
        const inst = 1000 / dt;
        this.fps = this.fps * 0.9 + inst * 0.1;
      }
    }
    this.last = nowMs;
    if (this.fps < 12) {
      if (this.lowSince === null) this.lowSince = nowMs;
      if (nowMs - this.lowSince >= 3000) this.degraded = true;
    } else {
      this.lowSince = null;
    }
  }

  reset(): void {
    this.fps = 60;
    this.degraded = false;
    this.last = null;
    this.lowSince = null;
  }
}
```

```ts
// src/main.ts — openCamera 함수만 교체 (boot 함수는 그대로 유지)
export async function openCamera(): Promise<HTMLVideoElement | null> {
  const video = document.getElementById('cam') as HTMLVideoElement | null;
  if (!video) return null;
  const attempts: MediaTrackConstraints[] = [
    { width: 1280, height: 720, facingMode: 'user' },
    { width: 640, height: 480 }
  ];
  for (const vc of attempts) {
    try {
      const stream = await navigator.mediaDevices.getUserMedia({ video: vc, audio: false });
      video.srcObject = stream;
      await video.play();
      return video;
    } catch {
      // 다음 해상도로 폴백
    }
  }
  return null;
}
```

주의: 기존 테스트가 `openCamera(video)` 형태로 video를 인자로 전달하므로, 실제 기존 시그니처를 먼저 확인할 것. 기존이 `openCamera(): Promise<HTMLVideoElement | null>` (인자 없음, 내부에서 getElementById)라면 camera.test.ts의 호출부를 `openCamera()`로 고치고, video 엘리먼트는 DOM에 `<video id="cam">`로 미리 추가할 것. 어느 쪽이든 시그니처는 변경하지 말고 테스트를 기존 시그니처에 맞출 것. 이 판단을 커밋 전 self-review에서 명시할 것.

- [ ] **Step 4: Run tests to verify they pass**

Run: `npx vitest run tests/fps-monitor.test.ts tests/camera.test.ts`
Expected: PASS (5 passed)

- [ ] **Step 5: Commit**

```bash
git add src/perf/fps-monitor.ts src/main.ts tests/fps-monitor.test.ts tests/camera.test.ts
git commit -m "feat: add fps monitor and camera fallback chain"
```

---

### Task 3: Calibration 소비 (반경 스케일·모드·존 표시)

**Files:**
- Modify: `src/game/fruit-ninja.ts` (radiusScale 필드 + hit 반경 적용)
- Modify: `src/game/body-abc.ts` (mode 필드)
- Modify: `src/ui/renderer.ts` (drawZones 추가)
- Test: `tests/calibration-apply.test.ts`

**Interfaces:**
- Consumes: `PoseMode` from `src/pose/types.ts`; 기존 Fruit/BodyABC 클래스 (필드 추가만, 기존 테스트 green 유지)
- Produces:
  - `FruitNinja.radiusScale: number` (default 1, hit 반경 `48 * radiusScale`)
  - `BodyABC.mode: PoseMode` (default 'standing')
  - `drawZones(canvas: HTMLCanvasElement, width: number): void` in `src/ui/renderer.ts` (3등분선 2개, `#f59e0b`, ctx 없으면 조용히 반환)

- [ ] **Step 1: Write the failing test**

```ts
// @vitest-environment happy-dom
// tests/calibration-apply.test.ts
import { describe, expect, it } from 'vitest';
import { FruitNinja } from '../src/game/fruit-ninja';
import { BodyABC } from '../src/game/body-abc';
import { drawZones } from '../src/ui/renderer';
import type { PoseFrame } from '../src/pose/types';

function wristFrame(x: number, y: number): PoseFrame {
  return {
    width: 640, height: 480, timestamp: 0,
    keypoints: [
      { name: 'left_wrist', x, y, score: 1 },
      { name: 'right_wrist', x: 500, y: 400, score: 1 }
    ]
  };
}

describe('calibration application', () => {
  it('fruit hit radius scales with radiusScale', () => {
    const g = new FruitNinja();
    g.start();
    g.radiusScale = 2;
    g.fruits.length = 0;
    g.fruits.push({ x: 100, y: 100, vx: 0, vy: 0, kind: 'fruit', alive: true });
    const events = g.tick(wristFrame(180, 100), 16);
    expect(events.some((e) => e.type === 'slice')).toBe(true);
  });
  it('body abc defaults to standing mode', () => {
    expect(new BodyABC().mode).toBe('standing');
  });
  it('drawZones does not throw without 2d context', () => {
    const canvas = document.createElement('canvas');
    expect(() => drawZones(canvas, 600)).not.toThrow();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run tests/calibration-apply.test.ts`
Expected: FAIL (radiusScale 없음 — `g.radiusScale = 2` 할당은 통과해도 hit 반경 미적용으로 slice 없음; drawZones 모듈 없음)

- [ ] **Step 3: Write minimal implementation**

```ts
// src/game/fruit-ninja.ts — 클래스에 필드 추가, hit 판정식 교체 (-su lines exact)
//   radiusScale = 1;
//   const hit = wrists.some((w) => w && Math.hypot(w.x - f.x, w.y - f.y) < 48 * this.radiusScale);
```

```ts
// src/game/body-abc.ts — 클래스에 필드 추가 (import type { PoseFrame, PoseMode } from '../pose/types';)
//   mode: PoseMode = 'standing';
```

```ts
// src/ui/renderer.ts — 함수 추가
import type { PoseFrame } from '../pose/types';

export function drawZones(canvas: HTMLCanvasElement, width: number): void {
  const ctx = canvas.getContext('2d');
  if (!ctx) return;
  ctx.save();
  ctx.strokeStyle = '#f59e0b';
  ctx.lineWidth = 3;
  ctx.setLineDash([10, 8]);
  for (const fx of [1 / 3, 2 / 3]) {
    ctx.beginPath();
    ctx.moveTo(width * fx, 0);
    ctx.lineTo(width * fx, canvas.height);
    ctx.stroke();
  }
  ctx.restore();
}
```

- [ ] **Step 4: Run tests to verify they pass (기존 스위트 포함)**

Run: `npx vitest run tests/calibration-apply.test.ts tests/fruit-ninja.test.ts tests/body-abc.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/game/fruit-ninja.ts src/game/body-abc.ts src/ui/renderer.ts tests/calibration-apply.test.ts
git commit -m "feat: apply calibration scale mode and zones"
```

---

### Task 4: GameLoop (rAF·dt클램프·디그레이드·HUD 콜백)

**Files:**
- Create: `src/game/loop.ts`
- Test: `tests/loop.test.ts`

**Interfaces:**
- Consumes: `PoseEngine` + `PoseFrame` (Task 3/후속T1), `Game`/`GameEvent` (Task 5), `ScoreBoard` (Task 5), `Calibration` (Task 2/4), `FpsMonitor` (후속T2), `drawSkeleton` (Task 5)
- Produces:
  - `type GameEventHandler = (events: GameEvent[], board: { score: number; combo: number }) => void` in `src/game/loop.ts`
  - `interface LoopGame extends Game { board: ScoreBoard }` in `src/game/loop.ts`
  - `class GameLoop` with `constructor(opts: { video: HTMLVideoElement | null; canvas: HTMLCanvasElement; engine: PoseEngine; game: LoopGame; calibration: Calibration; showSkeleton: boolean; onEvent?: GameEventHandler; onDegrade?: (fps: number) => void })`, `start(): void`, `stop(): void`, `readonly fps: number`

- [ ] **Step 1: Write the failing test**

```ts
// @vitest-environment happy-dom
// tests/loop.test.ts
import { afterEach, describe, expect, it, vi } from 'vitest';
import { GameLoop } from '../src/game/loop';
import { FakeEngine } from '../src/pose/fake-engine';
import { FruitNinja } from '../src/game/fruit-ninja';

let rafCb: FrameRequestCallback | null = null;
let cancelled = false;

function installRaf(): void {
  rafCb = null;
  cancelled = false;
  vi.stubGlobal('requestAnimationFrame', (cb: FrameRequestCallback) => {
    rafCb = cb;
    return 1;
  });
  vi.stubGlobal('cancelAnimationFrame', () => {
    cancelled = true;
  });
}

afterEach(() => {
  vi.unstubAllGlobals();
});

function pump(times: number, stepMs: number): void {
  let t = 1000;
  for (let i = 0; i < times; i++) {
    t += stepMs;
    const cb = rafCb;
    rafCb = null;
    cb?.(t);
  }
}

describe('GameLoop', () => {
  it('ticks game and forwards events with board state', () => {
    installRaf();
    const engine = new FakeEngine();
    engine.push({
      width: 640, height: 480, timestamp: 0,
      keypoints: [
        { name: 'left_wrist', x: 100, y: 100, score: 1 },
        { name: 'right_wrist', x: 500, y: 400, score: 1 }
      ]
    });
    const game = new FruitNinja();
    game.start();
    game.fruits.length = 0;
    game.fruits.push({ x: 100, y: 100, vx: 0, vy: 0, kind: 'fruit', alive: true });
    const seen: string[] = [];
    const canvas = document.createElement('canvas');
    canvas.width = 640;
    canvas.height = 480;
    const loop = new GameLoop({
      video: document.createElement('video'),
      canvas,
      engine,
      game,
      calibration: { scale: 1, centerX: 320, mode: 'standing', shoulderWidth: 200 },
      showSkeleton: false,
      onEvent: (events) => seen.push(...events.map((e) => e.type))
    });
    loop.start();
    pump(3, 16);
    loop.stop();
    expect(seen).toContain('slice');
    expect(cancelled).toBe(true);
  });
  it('clamps dt to 100ms', () => {
    installRaf();
    const engine = new FakeEngine();
    engine.push({ width: 640, height: 480, timestamp: 0, keypoints: [] });
    const game = new FruitNinja();
    game.start();
    const canvas = document.createElement('canvas');
    const loop = new GameLoop({
      video: null,
      canvas,
      engine,
      game,
      calibration: { scale: 1, centerX: 320, mode: 'seated', shoulderWidth: 100 },
      showSkeleton: false
    });
    loop.start();
    pump(1, 16);
    pump(1, 5000);
    loop.stop();
    expect(loop.fps).toBeGreaterThanOrEqual(0);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run tests/loop.test.ts`
Expected: FAIL with "Cannot find module '../src/game/loop'"

- [ ] **Step 3: Write minimal implementation**

```ts
// src/game/loop.ts
import type { PoseEngine } from '../pose/pose-engine';
import type { Calibration, PoseFrame } from '../pose/types';
import type { Game, GameEvent } from './types';
import type { ScoreBoard } from './engine';
import { FpsMonitor } from '../perf/fps-monitor';
import { drawSkeleton } from '../ui/renderer';

export type GameEventHandler = (events: GameEvent[], board: { score: number; combo: number }) => void;

export interface LoopGame extends Game {
  board: ScoreBoard;
}

export interface LoopOpts {
  video: HTMLVideoElement | null;
  canvas: HTMLCanvasElement;
  engine: PoseEngine;
  game: LoopGame;
  calibration: Calibration;
  showSkeleton: boolean;
  onEvent?: GameEventHandler;
  onDegrade?: (fps: number) => void;
}

export class GameLoop {
  private raf = 0;
  private running = false;
  private lastMs = 0;
  private monitor = new FpsMonitor();
  private frames = 0;
  private dummyVideo: HTMLVideoElement | null = null;
  private degradedNotified = false;
  constructor(private opts: LoopOpts) {}

  get fps(): number {
    return this.monitor.fps;
  }

  start(): void {
    if (this.running) return;
    this.running = true;
    this.monitor.reset();
    this.degradedNotified = false;
    this.lastMs = 0;
    const tick = async (nowMs: number): Promise<void> => {
      if (!this.running) return;
      this.raf = requestAnimationFrame(tick);
      if (this.lastMs === 0) this.lastMs = nowMs;
      const dt = Math.min(100, Math.max(0, nowMs - this.lastMs));
      this.lastMs = nowMs;
      this.monitor.sample(nowMs);
      if (this.monitor.degraded && !this.degradedNotified) {
        this.degradedNotified = true;
        this.opts.onDegrade?.(this.monitor.fps);
      }
      this.frames += 1;
      if (this.monitor.degraded && this.frames % 2 === 0) return;
      const video = this.opts.video ?? (this.dummyVideo ??= document.createElement('video'));
      let frame: PoseFrame;
      try {
        frame = await this.opts.engine.estimate(video);
      } catch {
        return;
      }
      const events = this.opts.game.tick(frame, dt);
      if (this.opts.showSkeleton) {
        try {
          drawSkeleton(this.opts.canvas, frame);
        } catch {
          // 렌더 실패는 루프를 멈추지 않음
        }
      }
      if (events.length > 0) {
        this.opts.onEvent?.(events, {
          score: this.opts.game.board.score,
          combo: this.opts.game.board.combo
        });
      }
    };
    this.raf = requestAnimationFrame(tick);
  }

  stop(): void {
    this.running = false;
    cancelAnimationFrame(this.raf);
  }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run tests/loop.test.ts`
Expected: PASS (2 passed). 주의: tick이 async라 estimate promise 해소가 다음 마이크로태스크에 일어남. pump 후 `await Promise.resolve()` 플러시가 필요하면 테스트에 `await new Promise((r) => setTimeout(r, 0))`를 pump 뒤에 추가해도 됨 (impl은 verbatim 유지, 테스트 하네스만). 이 경우를 리포트에 명시.

- [ ] **Step 5: Commit**

```bash
git add src/game/loop.ts tests/loop.test.ts
git commit -m "feat: add game loop with fps degrade"
```

---

### Task 5: App 배선 (팩토리·엔진선택·보정오버레이·HUD)

**Files:**
- Create: `src/ui/app.ts`
- Modify: `src/main.ts` (boot 확장 — 기존 nav/route/cam/stage 유지, HUD·오버레이·루프 배선 추가)
- Test: `tests/app.test.ts`

**Interfaces:**
- Consumes: `GameId` (router), 4 게임 클래스, `MoveNetAdapter`/`MediaPipeAdapter`/`FallbackEngine`/`FakeEngine`, `calibrate`+`isTPose`, `ScoreBoard`, `GameLoop`, `openCamera`, `shareLink`, `beep`
- Produces:
  - `createGame(id: Exclude<GameId, 'home'>): FruitNinja | SquatRunner | MathJump | BodyABC` in `src/ui/app.ts`
  - `selectEngineKind(cameraOk: boolean): 'camera' | 'fallback'` in `src/ui/app.ts`
  - `defaultCalibration(): Calibration` (`{ scale: 1, centerX: 320, mode: 'seated', shoulderWidth: 100 }`) in `src/ui/app.ts`

- [ ] **Step 1: Write the failing test**

```ts
// tests/app.test.ts
import { describe, expect, it } from 'vitest';
import { createGame, defaultCalibration, selectEngineKind } from '../src/ui/app';

describe('app factory', () => {
  it('creates four games with right ids', () => {
    expect(createGame('fruit').id).toBe('fruit');
    expect(createGame('squat').id).toBe('squat');
    expect(createGame('math').id).toBe('math');
    expect(createGame('abc').id).toBe('abc');
  });
  it('selects fallback without camera', () => {
    expect(selectEngineKind(false)).toBe('fallback');
    expect(selectEngineKind(true)).toBe('camera');
  });
  it('default calibration is seated instant-start', () => {
    expect(defaultCalibration()).toMatchObject({ mode: 'seated', scale: 1 });
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run tests/app.test.ts`
Expected: FAIL with "Cannot find module '../src/ui/app'"

- [ ] **Step 3: Write minimal implementation**

```ts
// src/ui/app.ts
import type { Calibration, PoseMode } from '../pose/types';
import type { GameId } from './router';
import { FruitNinja } from '../game/fruit-ninja';
import { SquatRunner } from '../game/squat-runner';
import { MathJump } from '../game/math-jump';
import { BodyABC } from '../game/body-abc';

export type PlayableId = Exclude<GameId, 'home'>;
export type PlayableGame = FruitNinja | SquatRunner | MathJump | BodyABC;

export function createGame(id: PlayableId): PlayableGame {
  if (id === 'squat') return new SquatRunner();
  if (id === 'math') return new MathJump();
  if (id === 'abc') return new BodyABC();
  return new FruitNinja();
}

export function selectEngineKind(cameraOk: boolean): 'camera' | 'fallback' {
  return cameraOk ? 'camera' : 'fallback';
}

export function defaultCalibration(): Calibration {
  return { scale: 1, centerX: 320, mode: 'seated' as PoseMode, shoulderWidth: 100 };
}
```

```ts
// src/main.ts — boot() 확장 (기존 boot 본문은 유지하고 아래 흐름을 추가; openCamera 함수는 Task 2 상태 유지)
import { parseHash } from './ui/router';
import type { GameId } from './ui/router';
import { createGame, defaultCalibration, selectEngineKind } from './ui/app';
import type { PlayableId } from './ui/app';
import { MoveNetAdapter } from './pose/movenet-adapter';
import { MediaPipeAdapter } from './pose/mediapipe-adapter';
import { FallbackEngine } from './pose/fallback-engine';
import type { PoseEngine } from './pose/pose-engine';
import { calibrate } from './calibration/calibrator';
import type { Calibration, PoseFrame } from './pose/types';
import { GameLoop } from './game/loop';
import { beep } from './ui/feedback';
import { drawZones } from './ui/renderer';
import { saveScore, shareLink, topScores } from './game/storage';

function gameIdOr(id: GameId): PlayableId {
  return id === 'home' ? 'fruit' : id;
}

async function collectCalibration(engine: PoseEngine, video: HTMLVideoElement | null, overlay: HTMLElement): Promise<Calibration> {
  overlay.textContent = 'T자세로 3초간 서주세요 (스킵 가능)';
  const frames: PoseFrame[] = [];
  const dummy = video ?? document.createElement('video');
  const started = performance.now();
  while (performance.now() - started < 3000 && frames.length < 60) {
    if ((overlay as HTMLElement & { skipped?: boolean }).skipped) break;
    try {
      frames.push(await engine.estimate(dummy));
    } catch {
      break;
    }
    await new Promise((r) => setTimeout(r, 50));
  }
  if (frames.length < 4) return defaultCalibration();
  return calibrate(frames);
}

export function boot(): void {
  if ((window as unknown as { __skelplay_booted?: boolean }).__skelplay_booted) return;
  (window as unknown as { __skelplay_booted?: boolean }).__skelplay_booted = true;
  const app = document.getElementById('app');
  if (!app) return;
  let loop: GameLoop | null = null;
  const render = () => {
    loop?.stop();
    loop = null;
    const id = gameIdOr(parseHash(window.location.hash));
    app.innerHTML =
      `<nav><a href="#/fruit">과일</a> <a href="#/squat">스쿼트</a> <a href="#/math">수학</a> <a href="#/abc">ABC</a></nav>` +
      `<p data-testid="route">${id}</p>` +
      `<video id="cam" playsinline muted></video><canvas id="stage" width="640" height="480"></canvas>` +
      `<p id="hud">준비 중…</p><p id="fps"></p><div id="calib"><button id="skip">스킵하고 시작</button></div>` +
      `<p>카메라 거부 시 키보드 모드: 방향키/WASD=이동, 스페이스=손들기, 포인터 이동도 가능</p>` +
      `<p>공유: <span id="share"></span></p><ol id="ranks"></ol>`;
    void start(id);
  };
  const start = async (id: PlayableId) => {
    const hud = document.getElementById('hud');
    const fpsEl = document.getElementById('fps');
    const overlay = document.getElementById('calib');
    const skip = document.getElementById('skip');
    skip?.addEventListener('click', () => {
      if (overlay) (overlay as HTMLElement & { skipped?: boolean }).skipped = true;
    });
    const game = createGame(id);
    const video = await openCamera();
    const cameraOk = video !== null;
    const kind = selectEngineKind(cameraOk);
    let engine: PoseEngine;
    if (kind === 'fallback' || (id === 'abc' && !cameraOk)) {
      const fb = new FallbackEngine();
      await fb.load();
      fb.attach(app);
      engine = fb;
    } else if (id === 'abc') {
      engine = new MediaPipeAdapter();
      try {
        await engine.load();
      } catch {
        const fb = new FallbackEngine();
        await fb.load();
        fb.attach(app);
        engine = fb;
      }
    } else {
      engine = new MoveNetAdapter();
      try {
        await engine.load();
      } catch {
        const fb = new FallbackEngine();
        await fb.load();
        fb.attach(app);
        engine = fb;
      }
    }
    const cal = overlay ? await collectCalibration(engine, video, overlay) : defaultCalibration();
    overlay?.remove();
    if (game instanceof FruitNinja) game.radiusScale = cal.scale;
    if (game instanceof BodyABC) game.mode = cal.mode;
    game.start();
    const canvas = document.getElementById('stage') as HTMLCanvasElement | null;
    if (!canvas) return;
    if (id === 'math') drawZones(canvas, 640);
    const share = document.getElementById('share');
    if (share) share.textContent = shareLink(id);
    const showRanks = () => {
      const ol = document.getElementById('ranks');
      if (ol) ol.innerHTML = topScores(id).map((s) => `<li>${s.name}: ${s.score}</li>`).join('');
    };
    showRanks();
    loop = new GameLoop({
      video,
      canvas,
      engine,
      game,
      calibration: cal,
      showSkeleton: true,
      onEvent: (events, board) => {
        for (const e of events) {
          beep(e.type === 'wrong' || e.type === 'bomb' ? 'miss' : e.type.startsWith('pose') || e.type === 'correct' ? 'win' : 'hit');
          if (hud) hud.textContent = `${e.label} — ${board.score}점 (콤보 ${board.combo})`;
        }
      },
      onDegrade: (fps) => {
        if (fpsEl) fpsEl.textContent = `저사양 모드 (${fps.toFixed(0)}fps, 15fps로 동작 중)`;
      }
    });
    loop.start();
    const fpsTimer = setInterval(() => {
      if (!loop) {
        clearInterval(fpsTimer);
        return;
      }
      if (fpsEl && !fpsEl.textContent?.startsWith('저사양')) fpsEl.textContent = `${loop.fps.toFixed(0)}fps · ${cal.mode === 'seated' ? '앉음' : '선'} 모드`;
    }, 500);
  };
  window.addEventListener('hashchange', render);
  render();
}

boot();
```

주의: 기존 main.ts의 boot-idempotent 가드·openCamera 체인(Task 2/9 산출)은 위 코드에 이미 포함되어 있음. 기존 파일과 충돌하는 부분(중복 openCamera 정의)은 교체할 것 — openCamera 함수는 Task 2 버전 그대로 두고 boot만 확장. self-review에서 기존 파일 diff와 정합 확인할 것.

- [ ] **Step 4: Run tests to verify they pass**

Run: `npx vitest run tests/app.test.ts && npx tsc --noEmit`
Expected: PASS + TSC CLEAN. 주의: boot()가 import 시 자동실행되므로 app.test.ts는 main.ts를 import하지 않음 — app.ts만 import. main.ts의 tsc 오류는 tsc로 확인.

- [ ] **Step 5: Commit**

```bash
git add src/ui/app.ts src/main.ts tests/app.test.ts
git commit -m "feat: wire playable app with calibration overlay and HUD"
```

---

### Task 6: 배포 문서 + 설계문서 정합 (md 기록)

**Files:**
- Create: `README.md`
- Create: `docs/DEPLOY.md`
- Create: `docs/QA-CHECKLIST.md`
- Create: `CHANGELOG.md`
- Modify: `docs/superpowers/specs/2026-09-19-skeleton-game-design.md` (폭탄감점·콤보*5·MediaPipe 매핑 반영)
- Test: 없음 (문서 태스크 — `npm run build` green으로 검증)

**Interfaces:**
- Consumes: 구현된 전체 (조작법·아키텍처 서술용)
- Produces: 4개 문서 + 설계문서 3곳 수정

- [ ] **Step 1: Write the documents (내용 verbatim 작성)**

README.md verbatim:

```md
# Skeleton Play — 카메라 운동 게임

랩탑·태블릿·크롬북 카메라로 즐기는 스켈레톤 운동 게임 4종. 설치 없이 브라우저에서 동작합니다.

## 시작

```bash
npm install
npm run dev
```

브라우저에서 `http://localhost:5173/#/fruit` 접속. 카메라 권한을 허용하세요.

## 게임

- `#/fruit` 과일 닌자 몸버전: 손(커서)으로 과일 베기, 폭탄은 건드리지 않기 (-15점)
- `#/squat` 스쿼트 러너: 앉았다 일어서기 반복
- `#/math` 점프 수학 퀴즈: 정답 구역으로 이동 + 손들기
- `#/abc` 몸으로 ABC: T·Y·O·L 순서대로 포즈 1초 유지

## 조작법

- 카메라: 화면 중앙에 전신이 보이게 1.5~2.5m
- 키보드: 방향키/WASD 이동, 스페이스 손들기 (카메라 없어도 완주 가능)
- 터치/마우스: 포인터 이동이 손 역할
- 보정: 시작 시 T자세 3초, 스킵 시 앉음 모드 기본값

## 트러블슈팅

- 카메라 거부됨 → 자동으로 키보드 모드
- 끊김 → 저사양 모드 토스트 확인, 조명을 밝게
- 모델 로드 실패 → 최초 1회는 온라인 필요 (CDN), 이후 PWA 캐시
```

docs/DEPLOY.md verbatim:

```md
# 배포 가이드 (정적 호스팅, 서버 없음)

## 공통

```bash
npm install
npm run build   # dist/ 생성
```

`dist/` 통째로 업로드. 환경변수 없음. HTTPS 필수 (카메라·PWA).

## GitHub Pages

`vite.config.ts`에 `base: '/<repo>/'` 추가 후 build, `dist/`를 `gh-pages` 브랜치에 푸시.

## Cloudflare Pages

빌드 명령 `npm run build`, 출력 디렉토리 `dist`, 프레임워크 프리셋 Vite.

## 학교 인트라넷

`dist/`를 내부 웹서버에 복사. 최초 모델 로드만 외부 CDN 필요:
- `@mediapipe/tasks-vision` wasm (jsdelivr)
- pose_landmarker_lite.task (googleapis)
이후 Service Worker가 캐시. 완전 오프라인이 필요하면 위 파일을 내부 경로에 두고 어댑터 URL 교체.
```

docs/QA-CHECKLIST.md verbatim:

```md
# QA 체크리스트 (실기기, 수동)

- [ ] 크롬북 × 앉음 × 1.5m: 15fps 이상, 판정 지연 체감 <0.2초
- [ ] 크롬북 × 선 × 2.5m: 스쿼트 10회 인식률 8/10 이상
- [ ] 윈도우 랩탑 × 선: 4종 완주
- [ ] 안드로이드 태블릿 가로모드: 상체모드 전환 확인
- [ ] iPad Safari: 카메라 권한·PWA 설치
- [ ] network 탭: 영상 외부 전송 없음 (mediapipe/tfjs CDN GET 제외하고 POST/WS 없음)
- [ ] 카메라 거부: 키보드 모드 자동 전환 + 4종 완주
- [ ] 저조도: 경고 토스트 확인
- [ ] fps<12 3초: 저사양 토스트 + 15fps 스킵
- [ ] T자세 보정 성공률, ABC 5자 인식률, 퀴즈 오입력률 기록
```

CHANGELOG.md verbatim:

```md
# Changelog

## 0.2.0 (2026-09-19) — 플레이 가능

- 플레이 루프: rAF·dt클램프·fps 저하 시 15fps 스킵·HUD
- 폴백: 키보드(WASD/방향키/스페이스)·포인터 합성 엔진
- 보정: T자세 3초 + 스킵 시 seated 기본값, 과일 반경 스케일·ABC 모드 보관
- 카메라: 720p→480p 폴백 체인, facingMode user
- MediaPipe canonical 네이밍 매핑으로 ABC 하이브리드 연결
- 문서: README·DEPLOY·QA-CHECKLIST·CHANGELOG

## 0.1.0 — 프로토타입

- 4종 게임 로직 + MoveNet/MediaPipe 어댑터 + 단위테스트 29개
```

설계문서 수정 3곳 (edit, 기존 문장 교체):

1. 과일닌자 항목에 폭탄 감점 추가: `폭탄은 양손 X자로 회피` → `폭탄은 양손 X자로 회피 (접촉 시 콤보 리셋 + 15점 감점)`. 판정로직 초안의 `아니면 감점` → `아니면 콤보 리셋 + board.add(-15) 15점 감점`.
2. §3 하이브리드 항목에 추가: `ScoreBoard 콤보 보너스는 +combo*5 (테스트가 계약). MediaPipe 어댑터는 landmarkName()으로 canonical 이름(shoulders/wrists 등)을 방출하여 BodyABC와 연결.`
3. §7 몸ABC 항목에 추가: `BodyABC.mode 필드로 seated/standing 보관 (HUD 표시, 향후 가중치 분기용).`

- [ ] **Step 2: Run build to verify nothing broke**

Run: `npm run build`
Expected: dist 생성 성공 (문서만 추가되므로 기존 테스트 무영향 — `npx vitest run`도 PASS여야 함)

- [ ] **Step 3: Commit**

```bash
git add README.md docs/DEPLOY.md docs/QA-CHECKLIST.md CHANGELOG.md docs/superpowers/specs/2026-09-19-skeleton-game-design.md
git commit -m "docs: pre-deploy guides and spec alignment"
```

---

## Self-Review

**1. Spec coverage:** A(FallbackEngine)→T1, B(FpsMonitor)→T2, C(openCamera)→T2, D(반경·모드·존)→T3, E(GameLoop)→T4, E(App)→T5, F(문서6종)→T6, 수락기준(완주·폴백·스킵·degraded·green·문서)→T4·T5·T6. 실기기 매트릭스 실행은 스펙 비범위(문서 인계)로 T6 체크리스트가 커버.

**2. Placeholder scan:** `TBD/TODO/implement later/적절한 에러 처리` 없음. Task 2의 openCamera 주의문은 implementer용 분기 지시 + self-review 명시 요구이므로 플레이스홀더 아님. Task 5의 `catch {}` 2곳은 폴백 체인 의도이며 빈 블록이 아님을 주석으로 명시 (수리됨).

**3. Type consistency:** `LoopGame extends Game { board: ScoreBoard }` — 4 게임 모두 `board = new ScoreBoard()` 공개 필드 보유 (T5/T6–T9 산출) → `createGame` 반환 `PlayableGame` 할당 가능. `Calibration` 필드명 (scale/centerX/mode/shoulderWidth) T2·T4와 일치. `GameEventHandler`의 board 인라인 타입 `{score,combo}`는 ScoreBoard의 getter 부분집합으로 호환. `drawZones(canvas, width)` 시그니처 T3 테스트와 일치. `openCamera` 무인자 시그니처 유지 (T2 주의문에 따라 테스트가 기존 시그니처에 맞춤).
